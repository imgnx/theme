# `theme` - OS Themefiles for individual directories

## AGENTS.md

Please make it so that when `theme roll` runs for the first time in a directory that it grabs the `dirname $PWD` and uses that as the default NAMESPACE. After the first time, it should re-use whatever is already in the .themefile.

Thank you,
Knifey McSlicington

